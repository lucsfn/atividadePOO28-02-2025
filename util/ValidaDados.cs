using System;

namespace Util
{
    public static class ValidaDados
    {
        public static bool ValidaCPF(string cpf)
        {
            if (cpf.Length != 11) return false;
            else
        }
    }
}